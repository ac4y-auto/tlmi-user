var Ac4yObject=require("@ac4y/ac4y-object/domain/Ac4yObject.js");

class Ac4yAlgebra extends Ac4yObject {

    constructor(object) {

        super(object);
        if (object != undefined && object === 0) return;
    } // constructor


    createSelf(object) {
        return new Ac4yAlgebra(object);

    } // createSelf



    rebuild(object, target) {

        target = target || this;
        super.rebuild(object, target);
        if (object) {


        } // if object does not empty

        return target;

    } // rebuild

    rebuilded(object, target) {
        return super.rebuilded(object, target);
    }

    copy(object, target) {

        target = target || this;
        super.copy(object, target);
        if (object) {


        } // if object does not empty

        return target;

    } // copy

} // Ac4yAlgebra

module.exports=Ac4yAlgebra
